services:
  - type: web
    name: employee-policy-agentic-ai

    runtime: python

    buildCommand: |
      pip install -r requirements.txt

    startCommand: |
      python main.py

    envVars:
      - key: GOOGLE_API_KEY
        sync: false